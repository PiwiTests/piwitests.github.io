import{_ as r,C as o,o as s,c as n,j as t,a as i,E as h,ak as l}from"./chunks/framework.sV-XtlWr.js";const v=JSON.parse('{"title":"Share links","description":"","frontmatter":{"title":"Share links","lang":"en-US"},"headers":[],"relativePath":"features/share-links.md","filePath":"features/share-links.md","lastUpdated":1790423679000}'),d={name:"features/share-links.md"};function c(p,e,u,g,k,f){const a=o("Needs");return s(),n("div",null,[e[0]||(e[0]=t("h1",{id:"share-links",tabindex:"-1"},[i("Share links "),t("a",{class:"header-anchor",href:"#share-links","aria-label":'Permalink to "Share links"'},"​")],-1)),h(a,{reporter:""}),e[1]||(e[1]=l('<p>A share link is a read-only URL that anyone can open — no dashboard account, no login. It opens one of four things:</p><ul><li>an <strong>execution</strong> or a <strong>failure cluster</strong>: the live counterpart of an <a href="./offline-export.html">offline export</a>, the investigation as it stands when the link is opened;</li><li>a <strong>report snapshot</strong>: a stored <a href="./quality-reports.html#report-snapshots">quality report</a>, as it was generated;</li><li>a <strong>saved dashboard</strong>: a <a href="#live-dashboard-links">live dashboard link</a>, computed at every view.</li></ul><p>Share links are <strong>off by default</strong>. Set <code>PIWI_SHARE_LINKS_ENABLED=true</code> to allow them — see the <a href="/reference/configuration.html#authentication">configuration reference</a>.</p><p>The <a href="https://piwitests.dev/demo/" target="_blank" rel="noreferrer">live demo</a> runs without a server, so it has no share links and hides the <strong>Share</strong> button.</p><h2 id="creating-a-link" tabindex="-1">Creating a link <a class="header-anchor" href="#creating-a-link" aria-label="Permalink to &quot;Creating a link&quot;">​</a></h2><p>On an execution page, a failure-cluster page or a report snapshot page, open <strong>Share</strong> (next to <strong>Export</strong> or <strong>Download</strong>); on a saved dashboard, open <em>Live dashboard links</em> in the dashboard&#39;s menu:</p><ol><li>Pick an expiry. The dialog offers lifetimes up to <code>PIWI_SHARE_LINK_MAX_TTL_DAYS</code> (30 days unless configured; setting it to <code>0</code> lifts the cap and allows links with no expiry).</li><li><strong>Copy the link immediately — it is shown only once.</strong> The server stores a hash of the token, not the token, so there is no way to display the URL again later. Mint a new link instead.</li></ol><p>Creating and revoking links requires the administrator or reporter role; any project member can see which links exist. Each entry in the dialog shows the link&#39;s prefix, its expiry state, and how many times it was opened.</p><h2 id="what-the-viewer-sees" tabindex="-1">What the viewer sees <a class="header-anchor" href="#what-the-viewer-sees" aria-label="Permalink to &quot;What the viewer sees&quot;">​</a></h2><p>The link serves the same self-contained HTML report the <a href="./offline-export.html">offline export</a> produces, rebuilt from the current data on every view — a diagnosis added after the link was minted shows up, a cluster marked fixed reads as fixed. The same size budgets apply (<code>PIWI_EXPORT_MAX_*</code>), so an anonymous view can never cost the server more than an authenticated export does.</p><p>Two consequences of &quot;live&quot; worth knowing:</p><ul><li><strong>Retention thins a shared cluster over time.</strong> When <a href="/operate/storage.html#data-retention">data retention</a> prunes old runs, a cluster link keeps resolving but progressively loses member evidence — the same thinning the dashboard shows. A link whose entity is pruned entirely answers 404. When the investigation must outlive retention, hand over an export file instead.</li><li><strong>Revocation is immediate.</strong> A revoked link stops resolving on the next request. Turning the feature off entirely (<code>PIWI_SHARE_LINKS_ENABLED</code> unset) dead-ends every outstanding link at once without deleting anything, so the variable doubles as a kill switch.</li></ul><h2 id="report-share-links" tabindex="-1">Report share links <a class="header-anchor" href="#report-share-links" aria-label="Permalink to &quot;Report share links&quot;">​</a></h2><p>A report snapshot&#39;s link renders the stored quality report as one self-contained HTML page. It shows the numbers the report was generated with, whatever retention deleted since, and answers 404 once the snapshot itself is pruned (<code>PIWI_RETENTION_REPORT_DAYS</code>).</p><p>A <a href="./quality-reports.html#report-schedules">report schedule</a> can mint one per report: turn on <strong>Share link</strong> in the schedule form. Each report then gets its own link, which the email and the Slack message carry beside the button to the snapshot, so a stakeholder without an account reads the report in one click. The link expires a week after the schedule&#39;s next report, within <code>PIWI_SHARE_LINK_MAX_TTL_DAYS</code>. The token travels encrypted with <code>PIWI_SECRET_KEY</code> from the schedule to the delivery, and is shown to nobody.</p><h2 id="live-dashboard-links" tabindex="-1">Live dashboard links <a class="header-anchor" href="#live-dashboard-links" aria-label="Permalink to &quot;Live dashboard links&quot;">​</a></h2><p>A live dashboard link renders a saved dashboard as a quality report computed at every view, and the page reloads itself every minute: a bookmark for a stakeholder, or a wall screen with nobody signed in. Built-in dashboards have none; duplicate one first.</p><ul><li>The numbers are computed with the project access of the person who minted the link, checked again at every view (at most a minute behind). The link dies when that access or the dashboard goes, when it expires, or when it is revoked.</li><li>It shows the aggregates and the lists the dashboard carries, never evidence files: no screenshot, no trace, no video.</li></ul><h2 id="status-badge" tabindex="-1">Status badge <a class="header-anchor" href="#status-badge" aria-label="Permalink to &quot;Status badge&quot;">​</a></h2><p>A report link and a live dashboard link each carry two images, shown when the link is minted:</p><ul><li><code>/share/&lt;token&gt;/badge.svg</code>: a badge such as &quot;tests on default branch · 97.8% · 7 d&quot;, the test pass rate on the pass-rate color scale, for a README or a wiki page. The dialog copies it as Markdown.</li><li><code>/share/&lt;token&gt;/chart.png</code>: the report&#39;s trend. A scheduled report&#39;s Slack message draws it as an image, since Slack needs a public address to show one.</li></ul><p>Both stop resolving with their link. An image proxy (GitHub&#39;s for a README, Slack&#39;s) may keep showing its copy for up to five minutes, the time the images may be cached. Images count against their link&#39;s request limit, not the reader&#39;s address, since such a proxy fetches for many readers.</p><h2 id="security-properties" tabindex="-1">Security properties <a class="header-anchor" href="#security-properties" aria-label="Permalink to &quot;Security properties&quot;">​</a></h2><ul><li>The token is a 256-bit random secret (<code>psl_</code> + 64 hex characters) — unguessable at any request rate. The server stores only its SHA-256 hash.</li><li>Everything a link serves is data its creator could already see: minting requires access to the execution, the cluster, every project of the snapshot, or the dashboard, so a link is a narrower delegation of an existing member&#39;s read access, never an escalation.</li><li>The rendered page is sandboxed into a unique origin and served with <code>noindex</code> and <code>Referrer-Policy: no-referrer</code>, so it cannot read dashboard cookies, call the API with credentials, or leak the URL through outbound links. The page is never cached; the two images may be, for five minutes.</li><li>A share URL is a capability: anyone holding it can view the page, and it can end up in browser history or proxy logs like any URL. Prefer short expiries, and revoke links when an investigation closes.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./offline-export.html">Offline export</a> — the file to hand over when the recipient has no network path to your instance</li><li><a href="/operate/authentication.html">Authentication</a> — roles, and who can mint or revoke</li><li><a href="/operate/storage.html#data-retention">Storage configuration</a> — retention, and how it interacts with long-lived links</li></ul>',26))])}const w=r(d,[["render",c]]);export{v as __pageData,w as default};

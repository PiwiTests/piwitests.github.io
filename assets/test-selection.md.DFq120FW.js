import{_ as s,o as t,c as i,ak as a}from"./chunks/framework.sV-XtlWr.js";const k=JSON.parse('{"title":"Test selections","description":"","frontmatter":{"title":"Test selections","lang":"en-US"},"headers":[],"relativePath":"test-selection.md","filePath":"test-selection.md","lastUpdated":1788464449000}'),n={name:"test-selection.md"};function o(h,e,r,l,d,p){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="test-selections" tabindex="-1">Test selections <a class="header-anchor" href="#test-selections" aria-label="Permalink to &quot;Test selections&quot;">​</a></h1><p>A <strong>selection</strong> is a named, data-driven subset of your suite that Piwi resolves from the run history it already keeps — then hands back as a command you can run. Instead of maintaining a <code>@smoke</code> tag convention and a grep pattern in a CI file that quietly drift apart, you describe the subset once (&quot;critical tests, under 15 s each, not quarantined&quot;) and <code>piwi run</code> turns it into the tests to run, every time, against the current state of the suite.</p><p>Selections are resolved on demand, never frozen: a definition is rules over catalog facts, so a renamed file or a newly flaky test is reflected the next time you resolve, with no edit.</p><h2 id="run-one" tabindex="-1">Run one <a class="header-anchor" href="#run-one" aria-label="Permalink to &quot;Run one&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span></span></code></pre></div><p><code>piwi run &lt;key&gt;</code> resolves the selection and runs <code>playwright test</code> with exactly those tests. Point it at your dashboard the same way the reporter does — <code>PIWI_DASHBOARD_URL</code>, <code>PIWI_API_KEY</code> (if auth is on), and <code>PIWI_PROJECT_NAME</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PIWI_DASHBOARD_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://piwi.example.com</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PIWI_PROJECT_NAME=web </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">npx </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">@piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --workers=4</span></span></code></pre></div><p>Anything after <code>--</code> is passed straight to Playwright. The run is <strong>stamped</strong> with the selection it came from, so the dashboard shows <code>smoke · 42 tests</code> instead of an anonymous filter, and a <a href="/ci.html">gate</a> can re-resolve the same definition to check nothing was silently dropped.</p><p>If the dashboard is unreachable, <code>piwi run</code> falls back to the full suite with a warning (and reuses the last good resolution from <code>.piwi/selection-cache.json</code> when it has one) — a reporting problem never breaks the test run. Pass <code>--strict</code> to invert that for CI, where an unresolvable selection should stop the pipeline. Resolving to <strong>zero</strong> tests is always an error: a smoke job that silently runs nothing is worse than one that fails loudly.</p><h3 id="print-instead-of-run" tabindex="-1">Print instead of run <a class="header-anchor" href="#print-instead-of-run" aria-label="Permalink to &quot;Print instead of run&quot;">​</a></h3><p><code>piwi select &lt;key&gt;</code> resolves and prints the Playwright arguments, for a composable two-step job:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> select</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --format</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .piwi-selected</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .piwi-selected</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>--format</code> picks the shape: <code>args</code> (<code>file:line</code> tokens, the default), <code>grep</code>, <code>files</code>, or <code>json</code> (the full resolution, including the matched tests, the estimate and any warnings). <code>--budget 5m</code> caps the total time for this resolution.</p><h3 id="balanced-shards" tabindex="-1">Balanced shards <a class="header-anchor" href="#balanced-shards" aria-label="Permalink to &quot;Balanced shards&quot;">​</a></h3><p><code>--shard i/n</code> keeps only shard <em>i</em> of <em>n</em>, split so each shard&#39;s summed test duration is even — historically informed balancing that Playwright&#39;s file-count sharding can&#39;t do. Because Piwi merges a run&#39;s shards into one, the merged run still covers the whole selection (so a <code>--require-selection</code> gate sees all of it):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --shard</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 2/4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --shard=2/4</span></span></code></pre></div><h3 id="fail-fast" tabindex="-1">Fail fast <a class="header-anchor" href="#fail-fast" aria-label="Permalink to &quot;Fail fast&quot;">​</a></h3><p><code>--fail-fast</code> emits the selection worst-first — the least-reliable tests (by pass rate) lead, so a likely failure surfaces early instead of after the stable bulk of the suite has run:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --fail-fast</span></span></code></pre></div><p>It reorders, it never drops: the same tests run, so the resolved hash is unchanged and a <code>--require-selection</code> gate still sees the whole set. Ordering is a hint — Playwright owns worker scheduling, so it influences file order rather than guaranteeing a strict sequence. It composes with <code>impact</code> (<code>piwi run impact --base origin/main --fail-fast</code>).</p><h2 id="built-in-selections" tabindex="-1">Built-in selections <a class="header-anchor" href="#built-in-selections" aria-label="Permalink to &quot;Built-in selections&quot;">​</a></h2><p>Two selections exist for every project with no setup:</p><table tabindex="0"><thead><tr><th>Key</th><th>What it resolves to</th></tr></thead><tbody><tr><td><code>failed</code></td><td>Tests whose most recent execution failed or timed out.</td></tr><tr><td><code>quarantine-free</code></td><td>The whole suite minus tests under an active <a href="/flaky-tests.html">quarantine</a>.</td></tr></tbody></table><p>They also double as worked examples of the definition format below.</p><h2 id="defining-a-selection" tabindex="-1">Defining a selection <a class="header-anchor" href="#defining-a-selection" aria-label="Permalink to &quot;Defining a selection&quot;">​</a></h2><p>Save a selection through the dashboard or the API (<code>POST /api/projects/:id/selections</code>). The <code>definition</code> is declarative JSON: OR-ed <code>include</code> groups, minus OR-ed <code>exclude</code> groups, then pins, a budget and a limit — applied in that order.</p><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;include&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;tags&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;smoke&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;priority&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;critical&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;high&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;maxAvgDurationMs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">15000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;exclude&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;quarantined&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;budget&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;maxTotalDurationMs&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">300000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;rankBy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;failureLikelihood&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;limit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">200</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Within a group every predicate must hold (AND); a test matches <code>include</code> if it matches any group (OR). An empty or absent <code>include</code> starts from the whole suite. The predicates, all optional:</p><table tabindex="0"><thead><tr><th>Predicate</th><th>Matches when</th></tr></thead><tbody><tr><td><code>tags</code> / <code>anyTags</code></td><td>the test carries all / any of these tags</td></tr><tr><td><code>owner</code>, <code>priority</code>, <code>feature</code></td><td>the <code>piwi:</code> annotation is one of these</td></tr><tr><td><code>files</code></td><td>the file path matches one of these globs (<code>**</code>, <code>*</code>, <code>?</code>)</td></tr><tr><td><code>suitePath</code>, <code>text</code></td><td>the describe chain / title (or file) contains this substring</td></tr><tr><td><code>quarantined</code>, <code>flaky</code>, <code>neverRun</code></td><td>the test is (or is not) in that state</td></tr><tr><td><code>minPassRate</code> / <code>maxPassRate</code></td><td>pass rate over executed runs is within bounds (0–1)</td></tr><tr><td><code>minAvgDurationMs</code> / <code>maxAvgDurationMs</code></td><td>average duration is within bounds</td></tr><tr><td><code>lastStatus</code></td><td>the latest execution&#39;s status is one of these</td></tr><tr><td><code>failedInLastRuns</code></td><td>the test failed within its last <em>N</em> executions (N ≤ 25)</td></tr></tbody></table><p>An unknown predicate is a validation error, not a silent no-op — a typo fails loudly rather than resolving to a wider set than you meant.</p><h3 id="budgets-and-pins" tabindex="-1">Budgets and pins <a class="header-anchor" href="#budgets-and-pins" aria-label="Permalink to &quot;Budgets and pins&quot;">​</a></h3><p><code>budget</code> turns a selection into a knapsack: tests are ranked (<code>failureLikelihood</code>, <code>recentFailure</code>, <code>priority</code>, <code>slowest</code> or <code>fastest</code>) and taken until their summed average duration hits <code>maxTotalDurationMs</code>. &quot;The best five minutes of this suite&quot; is an empty <code>include</code> plus a budget. <code>pins</code> force individual tests in (<code>add</code>) or out (<code>remove</code>) by test- case id, on top of whatever the predicates matched. <code>limit</code> caps the count last.</p><h2 id="from-a-playwright-config" tabindex="-1">From a Playwright config <a class="header-anchor" href="#from-a-playwright-config" aria-label="Permalink to &quot;From a Playwright config&quot;">​</a></h2><p>If you prefer <code>PIWI_SELECTION=smoke playwright test</code> over <code>piwi run</code>, resolve the selection in an ESM config with the <code>resolveSelection</code> helper (it needs top-level <code>await</code>, which <code>defineConfig</code> in an ESM config allows):</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineConfig } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@playwright/test&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { wrapConfig, resolveSelection } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@piwitests/reporter&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> selection</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> resolveSelection</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// reads PIWI_SELECTION; undefined when unset</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> wrapConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineConfig</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ grep: selection?.grep }));</span></span></code></pre></div><p>It stamps the run the same way <code>piwi run</code> does, and returns <code>undefined</code> (so the config runs everything) when no selection is named or the dashboard cannot be reached.</p><h2 id="guard-it-in-ci" tabindex="-1">Guard it in CI <a class="header-anchor" href="#guard-it-in-ci" aria-label="Permalink to &quot;Guard it in CI&quot;">​</a></h2><p>A tag convention can&#39;t tell you a smoke job silently shrank — a renamed file or an over-narrow grep quietly drops a test, and the job stays green. <code>piwi gate --require-selection &lt;key&gt;</code> closes that gap: the dashboard re-resolves the selection&#39;s current definition and fails the build if any test it now matches did not run, or ran and failed.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # run the subset, stamping the run</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gate</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --require-selection</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> smoke</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # then assert it held</span></span></code></pre></div><p>It composes with the other <a href="/ci.html">gate</a> rules (<code>--max-new-regressions</code>, <code>--fail-on-flaky</code>, …). A quarantined test is exempt — quarantine already means &quot;don&#39;t gate on this test&quot;.</p><h2 id="in-the-dashboard-and-for-agents" tabindex="-1">In the dashboard, and for agents <a class="header-anchor" href="#in-the-dashboard-and-for-agents" aria-label="Permalink to &quot;In the dashboard, and for agents&quot;">​</a></h2><p>The project&#39;s <strong>Selections</strong> tab lists the built-ins and your saved selections, with a builder that previews live what a definition resolves to — the matching tests, the estimated duration, any warnings, and the exact command — before you save it. For AI agents, the <a href="/mcp.html">MCP server</a> exposes <code>list_selections</code>, <code>resolve_selection</code> (key → tests + verify command), <code>preview_selection</code> (an ad-hoc definition, dry-run) and <code>analyze_selections</code> (health and drift); the <code>run-the-right-tests</code> skill ties them together.</p><h2 id="health-and-drift" tabindex="-1">Health and drift <a class="header-anchor" href="#health-and-drift" aria-label="Permalink to &quot;Health and drift&quot;">​</a></h2><p>A selection resolves fresh every time, which is the point — and also the risk: the set it runs can quietly change under you. A renamed file drops out of a <code>files</code> glob, a test turns flaky and falls below a <code>minPassRate</code>, and the job stays green while covering less than you think. The Selections tab makes that visible.</p><p>Each selection shows what it resolves to now, and a <strong>drifted</strong> badge when that differs from what its most recent <code>piwi run</code> recorded — the run stamped the hash and count it resolved then, so re-resolving and comparing catches a silent shrink (or growth) between runs. A quarantined-member count flags selections carrying tests whose verdict is already suspended.</p><p>Above the list, a <strong>coverage</strong> line answers the question a tag convention can&#39;t: how many tests are matched by <em>no</em> stored selection. Those &quot;unselected&quot; tests are the gap nobody can see today — nothing routine runs them as a named subset. Built-in selections don&#39;t count toward coverage (<code>quarantine-free</code> matches almost everything and would hide the signal). The same data is available to agents through the <code>analyze_selections</code> MCP tool.</p><h2 id="impact-from-diff" tabindex="-1">Impact-from-diff <a class="header-anchor" href="#impact-from-diff" aria-label="Permalink to &quot;Impact-from-diff&quot;">​</a></h2><p><code>piwi run impact --base &lt;ref&gt;</code> runs only the tests your change affects. The reporter computes the working-tree diff against <code>&lt;ref&gt;</code> locally (<code>git diff --name-only</code>), and the dashboard maps those files to tests through two observed edges:</p><ul><li><strong>Direct</strong> — a changed file that <em>is</em> a test file → the tests defined in it.</li><li><strong>Reach</strong> — a changed support file (a page object, helper, or app module) that a test&#39;s most recent execution actually ran through, per its captured source frames.</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @piwitests/reporter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> impact</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --base</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin/main</span></span></code></pre></div><p>It fails safe. A changed <em>source</em> file that maps to no test can&#39;t be proven irrelevant, so the run <strong>widens to the full suite</strong> with a warning rather than silently skipping it — impact never narrows away a test it&#39;s unsure about. A docs-only or config-only change impacts nothing and runs nothing. This is evidence-based impact, not static analysis: route- and page-level mapping (a changed server route → the tests that call it) needs a per-project config and is not attempted yet.</p><h2 id="suggestions" tabindex="-1">Suggestions <a class="header-anchor" href="#suggestions" aria-label="Permalink to &quot;Suggestions&quot;">​</a></h2><p>Piwi can <em>propose</em> selections and tags from the history it keeps — suggest-only, with the evidence attached, never applied. The <strong>Suggestions</strong> panel on the Selections tab (and the <code>suggest_selections</code> MCP tool) surface three kinds:</p><ul><li><strong><code>@slow</code> tags</strong> — tests whose average duration sits well past the suite&#39;s 95th percentile.</li><li><strong><code>@feature</code> tags</strong> — the dominant route family a test hits (say <code>checkout</code>) when it carries no <code>feature</code> annotation, inferred from the routes it actually called.</li><li><strong>A mined smoke suite</strong> — a greedy weighted set cover over <em>observed</em> route coverage under a time budget: it keeps picking the test that buys the most new routes per second until the budget is spent, producing the classic diminishing-returns curve. &quot;Save as selection&quot; turns the picks into a selection pinned to exactly those tests.</li></ul><p>Honest about what it is: coverage here means the routes a test was <em>seen</em> to hit on recent runs, not instrumented code coverage — an approximation, and a good one for smoke&#39;s job, which is breadth over entry points. A test must be stable (high pass rate, not flaky, not quarantined) to be mined into a smoke suite.</p><h2 id="what-selections-are-not" tabindex="-1">What selections are not <a class="header-anchor" href="#what-selections-are-not" aria-label="Permalink to &quot;What selections are not&quot;">​</a></h2><ul><li><strong>Not instrumented test-impact analysis.</strong> Predicates read <em>observed</em> history — durations, pass rates, statuses — not a build-time dependency graph.</li><li><strong>Not a way to hide failures.</strong> <a href="/flaky-tests.html">Quarantine</a> decides a test&#39;s verdict; a selection only decides whether it runs. The full suite stays your baseline — selections are for the fast loops in between.</li><li><strong>Not a hosted service.</strong> Everything resolves on your instance against your data.</li></ul>`,57)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};

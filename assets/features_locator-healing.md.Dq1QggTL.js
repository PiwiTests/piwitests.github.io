import{_ as s}from"./chunks/locator-healing-capture.CwzxfSiD.js";import{_ as o}from"./chunks/locator-healing.BjNtRxlF.js";import{_ as i,C as n,o as r,c as l,j as t,a as c,E as h,ak as d}from"./chunks/framework.sV-XtlWr.js";const p="/diagrams/locator-healing-resolution.svg",x=JSON.parse('{"title":"Locator healing","description":"","frontmatter":{"title":"Locator healing","lang":"en-US"},"headers":[],"relativePath":"features/locator-healing.md","filePath":"features/locator-healing.md","lastUpdated":1788720938000}'),g={name:"features/locator-healing.md"};function u(m,e,f,k,y,b){const a=n("Needs");return r(),l("div",null,[e[0]||(e[0]=t("h1",{id:"locator-healing",tabindex:"-1"},[c("Locator healing "),t("a",{class:"header-anchor",href:"#locator-healing","aria-label":'Permalink to "Locator healing"'},"​")],-1)),h(a,{reporter:"",fixtures:""}),e[1]||(e[1]=d('<p>When a locator stops matching — a button was renamed, an element moved, a hashed class changed — Piwi suggests concrete, ranked replacements instead of leaving you to guess.</p><p>While tests run, the <a href="/guide/capture-fixtures.html">capture fixtures</a> wrap Playwright&#39;s locator methods (<code>getByRole</code>, <code>getByTestId</code>, <code>locator</code>, …) and, after each successful action <strong>or passing web-first assertion</strong> (a passing <code>expect(locator).toBeVisible()</code> proves the element resolved just as a click does — so locators that are only ever asserted build healing history too), record the target element&#39;s attributes plus a list of alternative locators ranked by a stability score (<code>data-testid</code> = 100, role + accessible name ≈ 90, semantic CSS ≈ 35–40, hash-suffixed ≈ 10). Alongside the name-based alternatives, capture also generates <strong>structural, rename-proof</strong> ones — locators scoped to a stable ancestor (<code>getByTestId(&#39;signup-form&#39;).getByRole(&#39;textbox&#39;)</code> ≈ 72, <code>locator(&#39;#sidebar&#39;).getByRole(&#39;link&#39;)</code> ≈ 64, a document-unique landmark such as <code>getByRole(&#39;navigation&#39;).getByRole(&#39;link&#39;)</code> ≈ 55) and a name-free <code>getByRole</code> (≈ 58) when the element is the only one of its role on the page, or the only heading at its level (<code>getByRole(&#39;heading&#39;, { level: 1 })</code>). These keep working when a label or title changes, which breaks every name-derived locator at once. Heading locators carry their <code>level</code>, and the element&#39;s position among same-role elements is stored so a fully renamed element can still be re-identified on the failing page. Each candidate selector is probed against the live page for uniqueness — alternatives that would match several elements (strict-mode violations) are dropped at capture time. Live input <em>values</em> are never captured, so filled-in secrets can&#39;t leak into snapshots. One row per call site is upserted into the <code>locator_snapshots</code> table, so the latest known-good snapshot for every locator is always available.</p><figure><img src="'+s+'" alt="Diagram of the capture flow: a successful action or passing assertion goes through the capture proxy to an in-page element probe, which produces ranked alternative locators stored as one row per call site in the locator_snapshots table"><figcaption>Capture runs while tests pass: every locator that proves it resolves — through an action or an assertion — leaves behind ranked, uniqueness-checked replacements for the day it breaks.</figcaption></figure><p>When a locator later fails, the server resolves replacements through a ladder, most-trustworthy first:</p><ol><li><strong>Prior run</strong> — the exact call site (<code>file:line:col</code>) had a passing snapshot; its pre-captured alternatives are reused.</li><li><strong>Element match</strong> — the old element appears renamed or moved (its identity is gone from the failing page&#39;s ARIA snapshot), so <em>fresh</em> locators are generated for the element it most likely became. The match narrows heading candidates by <code>level</code> and, on a total rename with no shared words, falls back to the element&#39;s captured position among same-role elements (only when the same-role count is unchanged).</li><li><strong>Fingerprint</strong> — the call site shifted lines, but a locator-signature match finds the prior snapshot anyway.</li><li><strong>Cross-test</strong> — the same locator was captured by <em>another</em> test in the project (useful when the failing test has no capture history of its own for that locator — e.g. it fails on its very first run, or the history predates assertion capture); the freshest snapshot is reused.</li><li><strong>ARIA fallback</strong> — no prior snapshot exists; limited suggestions are derived from the failure-time ARIA snapshot (no HTML attributes).</li></ol><p>The ladder only runs for a <strong>resolution failure</strong> — the call log shows the locator never resolved (<code>waiting for &lt;locator&gt;</code> with no later <code>locator resolved to …</code> line), matched nothing (<code>resolved to 0 elements</code>), or matched several elements (a strict-mode violation). When the locator <em>did</em> resolve and the action or assertion failed afterwards (<code>locator resolved to 51 elements</code>, <code>element is not enabled</code>, a hidden element), or when the error is a navigation failure (<code>page.goto</code>, <code>net::ERR_*</code>), the panel shows one line — <em>The locator resolved; this is not a locator problem</em> — instead of a ranked menu, and no &quot;Locator fix&quot; signal appears on the run page. Rewriting a locator that already found its element would be a harmful edit.</p><p>When a stored snapshot is found but the element&#39;s captured accessible name is provably gone from the failing page (and no rename match was confident), the panel flags the list: name-based alternatives — including the failing locator itself — are kept visible but excluded from the recommendation, and candidates parsed from the failing page are shown alongside. This prevents the panel from &quot;recommending&quot; the very locator that just broke after a label or title change.</p><figure><img src="'+p+'" alt="Diagram of the healing resolution flow: the failing error is parsed into a locator signature and call site, matched through the stored-history ladder, sanity-checked against the failing page&#39;s ARIA snapshot (unchanged, renamed, or gone), and surfaced in the Locator fix panel"><figcaption>Healing runs from the failure&#39;s own error text: stored history is matched by call site, then signature, then across tests — and every hit is sanity-checked against the failing page before anything is recommended.</figcaption></figure><p>The result is shown as a <strong>Locator fix</strong> panel on the <a href="./evidence.html#one-execution-diagnosis-first">execution</a> and failure-cluster pages, and folded into the AI diagnosis context so the model recommends a grounded fix (see <a href="./ai-diagnosis.html#locator-healing">AI diagnosis</a>). A single <strong>recommended fix</strong> is highlighted — it keeps your original locator <em>style</em> where that style is stable enough (a minimal, idiomatic edit), and escalates to the sturdiest alternative (or advises adding a <code>data-testid</code>) only when the original style has nothing stable to fall back on.</p><h3 id="narrowing-a-strict-mode-violation-with-visible" tabindex="-1">Narrowing a strict-mode violation with <code>.visible()</code> <a class="header-anchor" href="#narrowing-a-strict-mode-violation-with-visible" aria-label="Permalink to &quot;Narrowing a strict-mode violation with `.visible()`&quot;">​</a></h3><p>When the failure is a <strong>strict-mode violation</strong> — the locator matched several elements — and only <strong>one of them is visible</strong>, the panel also suggests adding <code>.visible()</code> beside the replacement locators. Playwright 1.63&#39;s <code>locator.visible()</code> keeps only the visible matches, so <code>page.getByRole(&#39;button&#39;, { name: &#39;Pay&#39; }).visible().click()</code> resolves the ambiguity without changing the locator itself — the right fix when the duplicates are hidden variants (a loading or off-screen copy) rather than a naming problem. The visible-match count comes from the failure-time ARIA snapshot, which omits hidden nodes. The suggestion is shown only when the run&#39;s stored Playwright version is <strong>1.63 or later</strong> (older runs have no <code>.visible()</code> to add).</p><figure><img src="'+o+`" alt="Locator fix panel showing ranked replacement locators with stability scores and a recommended fix"><figcaption>The Locator fix panel — replacements ranked by stability score (data-testid ≈ 100, role + name ≈ 90), with a recommended fix and a copy button for each.</figcaption></figure><p>When the failing execution has an uploaded trace, the panel offers <strong>Pick from trace</strong>: it opens the trace in the dashboard&#39;s bundled <a href="./evidence.html#trace-viewer">trace viewer</a>, whose <em>Pick locator</em> tool works on the recorded page snapshots — so a replacement locator can be picked visually even for a CI failure nobody watched live. A replacement confirmed with the reporter&#39;s failure-time locator picker (<code>pickLocatorOnFailure</code>) shows a <strong>Your pick</strong> badge and becomes the recommended fix.</p><p>Capture adds a small cost in the test worker: one DOM read per call site, plus an ARIA snapshot only when the element&#39;s own attributes don&#39;t already settle its accessible name. Actions and passing assertions alike probe at most <strong>once per call site per test</strong> — a line re-run in a loop, a <code>toPass()</code> block, or a page-object method called repeatedly never probes twice. Negated assertions (<code>.not.…</code>), absence checks (<code>toBeHidden</code>, <code>toBeDetached</code>) and multi-element checks (<code>toHaveCount</code>, array forms) are never probed. Turn it off with <code>captureLocators: false</code> or <code>PIWI_CAPTURE_LOCATORS=false</code>; it is also disabled automatically whenever <code>collectPerformanceMetrics</code> is <code>false</code>.</p><blockquote><p>Healing is read-only — it never rewrites your test. It surfaces the replacement so you can apply it yourself.</p></blockquote><h2 id="inspect-the-failing-page-live-local-runs" tabindex="-1">Inspect the failing page live (local runs) <a class="header-anchor" href="#inspect-the-failing-page-live-local-runs" aria-label="Permalink to &quot;Inspect the failing page live (local runs)&quot;">​</a></h2><p>When a locator breaks while you&#39;re developing locally, the fastest fix is often to just look at the page. With <code>inspectOnFailure: true</code> (or <code>PIWI_INSPECT_ON_FAIL=true</code>), a failing test opens <strong>Piwi&#39;s own inspector overlay</strong> on its still-open page right before the browser would close — click any element to generate ranked, uniqueness-checked locators for it (with the same guided parent-anchoring described below), confirm one, and it&#39;s recorded just like a pick. This is Piwi&#39;s own overlay, <strong>not</strong> Playwright&#39;s native inspector, so anything you confirm flows back into the dashboard&#39;s healing data.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Linux / macOS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PIWI_INSPECT_ON_FAIL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">true</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --headed</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Windows (PowerShell)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$env:PIWI_INSPECT_ON_FAIL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;true&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --headed</span></span></code></pre></div><p><code>inspectOnFailure</code> opens the overlay on <strong>any</strong> failure so you can inspect the whole page; <code>pickLocatorOnFailure</code> (below) opens the <strong>same</strong> overlay but targeted at the locator that broke. Both are local debugging aids and deliberately conservative: they require a <strong>headed</strong> browser (<code>--headed</code> / <code>headless: false</code>), never activate under CI (any <code>CI</code> env var), skip expected failures (<code>test.fail()</code>), and with retries configured only open on the final attempt. While the overlay is open the run waits (the test timeout is lifted), so prefer <code>--workers=1</code> when enabling it.</p><h2 id="pick-a-replacement-locator-on-the-failing-page-local-runs" tabindex="-1">Pick a replacement locator on the failing page (local runs) <a class="header-anchor" href="#pick-a-replacement-locator-on-the-failing-page-local-runs" aria-label="Permalink to &quot;Pick a replacement locator on the failing page (local runs)&quot;">​</a></h2><p>One step beyond inspection: with <code>pickLocatorOnFailure: true</code> (or <code>PIWI_PICK_LOCATOR_ON_FAIL=true</code>), a test that failed on a locator gets Piwi&#39;s own picker injected into the still-open page — whether the failure was a <strong>locator action</strong> (<code>.click()</code>, <code>.fill()</code>, …) or an <strong>assertion</strong> (<code>expect(locator).toBeVisible()</code>). For an action, the broken locator and its call site come from the captured failure; for an assertion, they&#39;re read from Playwright&#39;s error (<code>Locator:</code> line + call site). The flow is guided, in three steps (Esc skips at any point):</p><ol><li><strong>Pick the element.</strong> Hovering highlights; the pick snaps to the nearest actionable ancestor (clicking the <code>&lt;span&gt;</code> inside a button picks the button), and <kbd>↑</kbd>/<kbd>↓</kbd> walk the DOM tree up/down before you click — the locator for the element under the cursor is shown in a chip pinned to it and again on its own line in the banner, so each step of the walk shows what it would produce.</li><li><strong>Bless stable parents (optional).</strong> The element&#39;s ancestors are listed with their strongest hook (<code>data-testid</code>, <code>#id</code>, labeled landmark, role). Select one or more to scope the locator to — hovering a row outlines that parent in the page and names it in a chip pinned to it, and a live <strong>&quot;matches N&quot;</strong> count is recomputed against the real failing page on every toggle (exactly 1 = green). Selected parents produce anchor-scoped chains like <code>getByTestId(&#39;signup-form&#39;).getByRole(&#39;button&#39;)</code> — the rename-proof style — and picking several adds a combined chain when it isolates exactly one element.</li><li><strong>Confirm.</strong> The ranked, uniqueness-checked candidates (standard generation merged with your anchor-scoped chains) are listed; pick one to confirm it.</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Linux / macOS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PIWI_PICK_LOCATOR_ON_FAIL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">true</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --headed</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Windows (PowerShell)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$env:PIWI_PICK_LOCATOR_ON_FAIL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;true&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --headed</span></span></code></pre></div><p>A confirmed pick is recorded in three places:</p><ul><li><strong>The run&#39;s locator snapshots</strong> — the pick is folded into the failing call site&#39;s snapshot (flagged <code>pickedByUser</code>, listed first), so after the run uploads, the <strong>Locator fix</strong> panel for that failure shows your confirmed choice at the top.</li><li><strong>A <code>piwi-user-pick</code> attachment</strong> and a report <strong>annotation</strong>, so the choice is visible in the Playwright report and trace.</li><li><strong>The terminal</strong>, with the failing call site (<code>file:line:col</code>) and the replacement, ready to paste into the test.</li></ul><p>The gate is identical to <code>inspectOnFailure</code> (headed browser, never under CI, final attempt only), and the picker suppresses the page&#39;s own click handlers while active, so picking can&#39;t navigate or mutate the failing page. Picking never rewrites your test — it records the choice so you (or the dashboard) can apply it.</p><p>The same picker engine also ships as the <a href="/features/extension.html">Piwi Picker browser extension</a> — pick ranked, uniqueness-checked locators from any live page in Chrome or Edge, with no test run and no server required.</p><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="/guide/capture-fixtures.html">Capture fixtures</a> — the one-file setup that records the locator snapshots healing ranks from</li><li><a href="./auto-heal.html">Auto-heal PRs</a> — when Piwi opens the recommended locator fix as a pull request itself</li><li><a href="./fix-plans.html">Fix plans, reproduce &amp; bisect</a> — where the locator fix sits in the whole plan</li><li><a href="/features/extension.html">Browser extension</a> — the same picker on any live page, no run needed</li><li><a href="/guide/concepts.html#locator-snapshot">Core concepts</a> — what a locator snapshot stores</li></ul>`,29))])}const C=i(g,[["render",u]]);export{x as __pageData,C as default};

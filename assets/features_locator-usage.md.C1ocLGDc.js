import{_ as t,o,c as s,ak as a}from"./chunks/framework.sV-XtlWr.js";const n="/screenshots/execution-locators.png",p=JSON.parse('{"title":"Who uses a locator","description":"Before you change an element — rename a label, remove a data-testid, replace a <select> with an autocomplete — you can see which tests reach it and from which lines.","frontmatter":{"title":"Who uses a locator","lang":"en-US","head":[["link",{"rel":"canonical","href":"https://piwitests.dev/features/locator-usage.html"}],["meta",{"property":"og:url","content":"https://piwitests.dev/features/locator-usage.html"}],["meta",{"property":"og:title","content":"Who uses a locator | Piwi Dashboard"}],["meta",{"property":"og:description","content":"Before you change an element — rename a label, remove a data-testid, replace a <select> with an autocomplete — you can see which tests reach it and from which lines."}]]},"headers":[],"relativePath":"features/locator-usage.md","filePath":"features/locator-usage.md","lastUpdated":1790452519000}'),r={name:"features/locator-usage.md"};function i(h,e,c,l,d,u){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="who-uses-a-locator" tabindex="-1">Who uses a locator <a class="header-anchor" href="#who-uses-a-locator" aria-label="Permalink to &quot;Who uses a locator&quot;">​</a></h1><p>Before you change an element — rename a label, remove a <code>data-testid</code>, replace a <code>&lt;select&gt;</code> with an autocomplete — you can see which tests reach it and from which lines.</p><p>Playwright reports the full locator chain on every locator step: <code>getByRole(&#39;form&#39;, { name: &#39;Shipping&#39; }).getByLabel(&#39;Country&#39;)</code>, not only the last call. Piwi already stores those steps for every execution, passing ones included, so it indexes each chain with the action that used it (<code>Select option</code>, <code>Expect toHaveValue</code>, …) and the call site (<code>file:line:col</code>, often a page-object method). Nothing extra is captured while tests run: the index works with the reporter alone, and with <a href="./locator-healing.html">locator healing</a> turned off.</p><h2 id="the-locators-tab" tabindex="-1">The Locators tab <a class="header-anchor" href="#the-locators-tab" aria-label="Permalink to &quot;The Locators tab&quot;">​</a></h2><p>An execution&#39;s evidence card has a <strong>Locators</strong> tab listing every locator the test used, in order, with the action and the call site. The count beside each one is how many tests in the project use the same chain. A test case&#39;s page shows the same list for its latest execution.</p><figure><img src="'+n+'" alt="The Locators tab of an execution: each locator the test used, numbered in step order, with its action above it and a button giving how many tests use the same chain"><figcaption>Every locator an execution used, in step order.</figcaption></figure><p>Opening a count answers <strong>Who uses this?</strong> in three ways:</p><ul><li><strong>This locator</strong> — tests whose steps used this exact chain.</li><li><strong>Same target</strong> — chains ending on the same call inside any container, so <code>getByLabel(&#39;Country&#39;)</code> on its own and inside the shipping form both count.</li><li><strong>Inside its container</strong> — every chain that searches inside one of the chain&#39;s containers. Changing the container itself (removing a wrapper, renaming a form) reaches all of them, even when no test targets the container directly.</li></ul><p>Call sites shared by the most tests come first: a page-object line used by five tests is one fix for all five. <strong>Run these tests</strong> turns the list into a <code>npx playwright test</code> command through the same materialization as <a href="/guide/test-selection.html">test selection</a>.</p><h2 id="the-locators-page" tabindex="-1">The Locators page <a class="header-anchor" href="#the-locators-page" aria-label="Permalink to &quot;The Locators page&quot;">​</a></h2><p>A project&#39;s page links to <strong>Locators</strong> from its <strong>More actions</strong> menu, and an execution&#39;s <strong>Locators</strong> tab links to it too. It answers the question from the other side: not &quot;what does this test use?&quot; but &quot;does any test use this?&quot;.</p><p><strong>Check locators</strong> takes locators pasted one per line, as the <a href="./extension.html">Piwi Picker extension</a>&#39;s <strong>Copy all</strong> copies them for an element, or lines of test code (<code>await page.getByLabel(&#39;Card number&#39;).fill(&#39;4242…&#39;)</code>: the locator is read out of each line). Each locator gets a verdict, from the closest match found:</p><ul><li><strong>Used by N tests</strong> — the exact chain is in the index.</li><li><strong>Same target in N tests</strong> — the same last call, inside other containers.</li><li><strong>A similar locator in N tests</strong> — the same method with a looser argument that finds the same element: a test&#39;s <code>getByRole(&#39;button&#39;, { name: /pay/i })</code> or <code>getByText(&#39;Pay&#39;)</code> finds the pasted <code>getByRole(&#39;button&#39;, { name: &#39;Pay now&#39; })</code> or <code>getByText(&#39;Pay now&#39;)</code>, following Playwright&#39;s case-insensitive substring matching when <code>exact</code> is not set.</li><li><strong>A container in N tests</strong> — the locator is a container that other chains search inside.</li></ul><p>Below the verdicts, the tests reaching any of the pasted locators are listed once, with how many of the locators each one reaches. Paste every locator the extension gives for an element, and that list answers whether the element is tested, whichever way the tests wrote its locator. The page keeps what you paste in its URL (<code>?q=</code>), so a check can be shared as a link; the extension&#39;s <strong>Find these locators in Piwi ↗</strong> opens it that way.</p><p><strong>Locators your tests use</strong> lists every chain of the index, the ones shared by the most tests first, with a filter. Each count opens <strong>Who uses this?</strong>.</p><p>The index behind this page is also what the extension&#39;s <a href="./tested-elements.html">Tested elements</a> overlay evaluates on a live page.</p><h2 id="branches" tabindex="-1">Branches <a class="header-anchor" href="#branches" aria-label="Permalink to &quot;Branches&quot;">​</a></h2><p>Each use is stored with the branch of the run that recorded it. A run on the project&#39;s default branch, or with no branch, records the default branch&#39;s uses; a run on any other branch records that branch&#39;s own. A feature branch that renames a label never hides the old label&#39;s tests on <code>main</code>, and <code>main</code> only loses a use once its own runs stop making it. The default branch is the one Piwi knows for the project, the most common branch among its runs otherwise, as for <a href="./branches.html">branch baselines</a>.</p><p>The <strong>Locators</strong> page, <strong>Who uses this?</strong> and the extension&#39;s <a href="./tested-elements.html">Tested elements</a> read one branch at a time:</p><ul><li><strong>The default branch</strong>, unless another is chosen.</li><li><strong>A branch</strong>: the tests that ran on it count with what they did there, and the others with what they do on the default branch, since a test that did not run on a branch is taken to be unchanged on it. This is per Playwright project: a test that ran only in <code>chromium</code> on the branch keeps its default-branch uses in <code>firefox</code>.</li><li><strong>All branches</strong>: every use recorded on any branch, which answers &quot;does any run of the project still use this locator?&quot;.</li></ul><p>An execution&#39;s <strong>Locators</strong> tab counts in the branch of its own run. The index lists at most the 50 branches seen most recently; the uses of a branch stay until the index is rebuilt.</p><h2 id="requirements-and-limits" tabindex="-1">Requirements and limits <a class="header-anchor" href="#requirements-and-limits" aria-label="Permalink to &quot;Requirements and limits&quot;">​</a></h2><ul><li><strong>Playwright 1.61 or later</strong>, with step collection on (<code>collectPerformanceMetrics</code>, the default). Playwright 1.63 puts the chain in each step&#39;s parameters; 1.61 and 1.62 print it in the step title, which Piwi reads the same way.</li><li><strong>Getter calls are not reported.</strong> Playwright hides <code>textContent()</code>, <code>inputValue()</code>, <code>isVisible()</code> and similar calls from reporters, so a locator used only through them is not in the index.</li><li><strong>Long and described chains.</strong> Piwi keeps the first 500 steps of an execution (<code>PIWI_INGEST_MAX_STEPS</code>) and the first 200 characters of each step parameter (<code>PIWI_INGEST_MAX_STEP_PARAM_VALUE_CHARS</code>), and <code>locator.describe()</code> replaces the chain with its description. Chains cut short or described are skipped.</li><li><strong>Matching is by chain text.</strong> Two different chains that happen to resolve to the same element count separately. The counts tell you where to look, not that a test will break. To match on the element itself, open the page and use the extension&#39;s <a href="./tested-elements.html">Tested elements</a>, which evaluates every chain there.</li><li><strong>The index fills itself.</strong> New runs are indexed as they arrive. When the server starts, it indexes each project&#39;s stored history once: for every test, Playwright project and branch, the latest passed execution, or the latest one when none passed. The rebuild endpoint empties a project&#39;s index and indexes its stored history again, which also splits by branch the uses indexed before branches were recorded (until then they count as the default branch&#39;s): see the <a href="https://piwitests.dev/demo/docs" target="_blank" rel="noreferrer">API docs</a>, or <code>/docs</code> on your own instance.</li></ul>',23)])])}const m=t(r,[["render",i]]);export{p as __pageData,m as default};

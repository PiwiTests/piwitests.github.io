import{_ as t,o,c as r,ak as s}from"./chunks/framework.sV-XtlWr.js";const g=JSON.parse('{"title":"Failure clusters & the inbox","description":"","frontmatter":{"title":"Failure clusters & the inbox","lang":"en-US"},"headers":[],"relativePath":"failure-clusters.md","filePath":"failure-clusters.md","lastUpdated":1788680695000}'),a={name:"failure-clusters.md"};function n(i,e,d,l,u,h){return o(),r("div",null,[...e[0]||(e[0]=[s('<h1 id="failure-clusters-the-inbox" tabindex="-1">Failure clusters &amp; the inbox <a class="header-anchor" href="#failure-clusters-the-inbox" aria-label="Permalink to &quot;Failure clusters &amp; the inbox&quot;">​</a></h1><p>A run gives you a list of failures. The same root cause usually breaks several tests, and the same break comes back run after run — so a list of failures is really a much shorter list of <em>problems</em>, repeated. Piwi groups failures by that root cause into <strong>failure clusters</strong>, and the Home page turns those clusters into a <strong>failure inbox</strong>: a queue of the problems you still owe a decision, not a wall of red to re-read every morning.</p><p>For how clusters are formed (the error fingerprint) and diagnosed, see <a href="./ai-diagnosis.html#failure-clustering">AI diagnosis &amp; clustering</a>.</p><h2 id="the-failure-inbox" tabindex="-1">The failure inbox <a class="header-anchor" href="#the-failure-inbox" aria-label="Permalink to &quot;The failure inbox&quot;">​</a></h2><p>The <strong>Failure inbox</strong> on Home lists every open failure cluster across the projects you can see, newest first. Each row is one cluster:</p><ul><li>the <strong>headline</strong> — the failure in one line;</li><li>the <strong>top clue</strong> — a muted one-line hint at the likely cause (a timeout on a locator, a strict-mode match, a fix that regressed);</li><li>the <strong>owner or assignee</strong> — who the failure belongs to (see <a href="#owners-and-assignees">Owners</a> below);</li><li>the <strong>age</strong> and the <strong>affected-test count</strong>;</li><li>the exceptional <strong>badges</strong> only — a regression on the default branch, a fix that didn&#39;t hold, a quarantined cluster, a &quot;snoozed, back&quot; marker, a pending merge suggestion.</li></ul><h3 id="queues" tabindex="-1">Queues <a class="header-anchor" href="#queues" aria-label="Permalink to &quot;Queues&quot;">​</a></h3><p>The inbox is split into queues, each with a live count. Pick one to focus; <strong>All open</strong> is the default, and the queue is shareable — it lives in the URL as <code>?queue=</code>.</p><table tabindex="0"><thead><tr><th>Queue</th><th>What it holds</th></tr></thead><tbody><tr><td><strong>All open</strong></td><td>Every open, non-snoozed cluster.</td></tr><tr><td><strong>New</strong></td><td>Clusters first seen — or seen again — since you last opened the inbox (kept per browser, no account needed).</td></tr><tr><td><strong>Mine</strong></td><td>Clusters whose assignee, or whose derived owner, is you.</td></tr><tr><td><strong>Regressions</strong></td><td>Clusters that regressed on the project&#39;s default branch and are still failing there.</td></tr><tr><td><strong>Fix didn&#39;t hold</strong></td><td>Clusters whose fix landed and then regressed.</td></tr><tr><td><strong>Quarantine ready</strong></td><td>Quarantined clusters whose tests have stopped failing — safe to release.</td></tr><tr><td><strong>Merge suggestions</strong></td><td>Clusters that are part of a pending merge suggestion awaiting a decision (see <a href="./ai-diagnosis.html#failure-clustering">clustering</a>).</td></tr></tbody></table><h3 id="triage-from-the-row-or-the-keyboard" tabindex="-1">Triage from the row or the keyboard <a class="header-anchor" href="#triage-from-the-row-or-the-keyboard" aria-label="Permalink to &quot;Triage from the row or the keyboard&quot;">​</a></h3><p>Every row can be triaged in place, and the whole inbox is keyboard-driven. Select a row with the mouse or <code>j</code> / <code>k</code>, then:</p><table tabindex="0"><thead><tr><th>Key</th><th>Action</th></tr></thead><tbody><tr><td><code>o</code></td><td>Open the cluster</td></tr><tr><td><code>r</code></td><td>Resolve</td></tr><tr><td><code>i</code></td><td>Ignore</td></tr><tr><td><code>q</code></td><td>Quarantine the cluster&#39;s tests</td></tr><tr><td><code>a</code></td><td>Assign</td></tr><tr><td><code>s</code></td><td>Snooze</td></tr><tr><td><code>l</code></td><td>Link a known issue</td></tr><tr><td><code>x</code></td><td>Select the row (<code>shift</code>+<code>j</code>/<code>k</code> extends the selection, <code>esc</code> clears it)</td></tr></tbody></table><p>With one or more rows selected, a <strong>bulk bar</strong> applies resolve, ignore, assign, snooze or quarantine to all of them at once. Every action is optimistic and undoable for a few seconds — a toast with <strong>Undo</strong> reverses it.</p><p>The keyboard hints are hidden on touch devices, where the row&#39;s action buttons are always visible.</p><h2 id="owners-and-assignees" tabindex="-1">Owners and assignees <a class="header-anchor" href="#owners-and-assignees" aria-label="Permalink to &quot;Owners and assignees&quot;">​</a></h2><p>A cluster&#39;s <strong>owner</strong> is derived, not stored: it comes from the failing test&#39;s <code>piwi:owner</code> annotation, or falls back to the repository&#39;s <code>CODEOWNERS</code>. You can override it by <strong>assigning</strong> the cluster to a person — an assignee takes precedence over the derived owner, and the <strong>Mine</strong> queue matches either one against the signed-in user (by name or email, best effort).</p><h2 id="snoozing" tabindex="-1">Snoozing <a class="header-anchor" href="#snoozing" aria-label="Permalink to &quot;Snoozing&quot;">​</a></h2><p>Some failures are real but not now — a known flake you&#39;re waiting to reproduce, a break you&#39;ll get to next sprint. <strong>Snoozing</strong> hides a cluster from every inbox queue without touching its status: a snoozed cluster is still <em>open</em>, just out of sight.</p><p>Three durations are offered:</p><ul><li><strong>1 day</strong> and <strong>1 week</strong> — the cluster returns when the deadline passes.</li><li><strong>Until it recurs</strong> — the cluster stays hidden until a new run fails it again, then returns to the <strong>New</strong> queue with a <strong>snoozed, back</strong> badge so you know it woke on its own.</li></ul><p>Snooze never changes a cluster&#39;s triage status, and a snoozed cluster does not count as &quot;failing now&quot; in the project health and portfolio views. The cluster page shows the snooze state in its triage control, with an <strong>Unsnooze</strong> action to bring it back immediately.</p><h2 id="from-an-ai-agent" tabindex="-1">From an AI agent <a class="header-anchor" href="#from-an-ai-agent" aria-label="Permalink to &quot;From an AI agent&quot;">​</a></h2><p>The <a href="./mcp.html">MCP server</a> exposes the same queue: <code>list_open_clusters</code> takes an optional <code>queue</code> argument (<code>mine</code>, <code>regressions</code>, <code>fix-didnt-hold</code>, <code>quarantine-ready</code>, <code>merge-suggestions</code>) so an agent can pull the same focused list the dashboard shows, then triage with <code>set_cluster_status</code>.</p>',23)])])}const f=t(a,[["render",n]]);export{g as __pageData,f as default};

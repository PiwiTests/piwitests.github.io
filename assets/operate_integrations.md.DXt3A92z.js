import{_ as t,o as n,c as a,ak as s}from"./chunks/framework.sV-XtlWr.js";const o="/screenshots/integrations-settings.png",p=JSON.parse('{"title":"Integrations","description":"","frontmatter":{"title":"Integrations","lang":"en-US"},"headers":[],"relativePath":"operate/integrations.md","filePath":"operate/integrations.md","lastUpdated":1789561280000}'),i={name:"operate/integrations.md"};function r(c,e,d,l,h,u){return n(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="integrations" tabindex="-1">Integrations <a class="header-anchor" href="#integrations" aria-label="Permalink to &quot;Integrations&quot;">​</a></h1><p>Connect an issue tracker and the external links you pin to a run, execution or failure cluster stop being dead URLs: Piwi fetches the ticket&#39;s title and status through the connection and keeps the badge current when you refresh. Jira Cloud is the first tracker; the connection layer is shaped so more follow.</p><p>A connection is instance infrastructure — one row per external system, managed by an administrator, shared by every project. Nothing is sent to Jira until a connection exists, and credentials are encrypted at rest with <code>PIWI_SECRET_KEY</code>.</p><div class="doc-screenshot"><img src="'+o+'" alt="Settings → Integrations: a Jira card with a connected system, its verified status, and a test-connection button"></div><h2 id="connecting-jira-cloud" tabindex="-1">Connecting Jira Cloud <a class="header-anchor" href="#connecting-jira-cloud" aria-label="Permalink to &quot;Connecting Jira Cloud&quot;">​</a></h2><p>Jira Cloud authenticates with an account email and an API token (REST v3, HTTP Basic).</p><ol><li>Create an API token at <a href="https://id.atlassian.com/manage-profile/security/api-tokens" target="_blank" rel="noreferrer">id.atlassian.com</a> under <strong>Security → API tokens</strong>, signed in as the account whose permissions Piwi should act with.</li><li>Open <strong>Settings → Integrations</strong> (administrator only) and connect Jira with: <ul><li><strong>Base URL</strong> — your site, e.g. <code>https://your-team.atlassian.net</code>.</li><li><strong>Account email</strong> — the Atlassian account the token belongs to.</li><li><strong>API token</strong> — the token from step 1.</li></ul></li><li>Click <strong>Test connection</strong>. Piwi calls <code>GET /rest/api/3/myself</code> and shows the account it resolved to, so you can confirm the credentials before relying on them.</li></ol><p>Once connected, a Jira link — <code>https://your-team.atlassian.net/browse/PROJ-123</code>, including a self-hosted host once its connection exists — is recognized as a Jira issue, unfurls with the summary and a status badge (colored by the issue&#39;s status category), and refreshes through the connection.</p><h2 id="environment-managed-vs-dashboard-managed" tabindex="-1">Environment-managed vs dashboard-managed <a class="header-anchor" href="#environment-managed-vs-dashboard-managed" aria-label="Permalink to &quot;Environment-managed vs dashboard-managed&quot;">​</a></h2><p>A connection can live in either place:</p><ul><li><strong>Dashboard-managed</strong> — added on the Integrations page. Editable there; the credential is encrypted in the database and never returned by any endpoint. Saving with the token field left blank keeps the stored token.</li><li><strong>Environment-managed</strong> — created from the environment at startup when <code>PIWI_JIRA_BASE_URL</code>, <code>PIWI_JIRA_EMAIL</code> and <code>PIWI_JIRA_API_TOKEN</code> are all set. It shows a lock badge and is read-only in the dashboard: change it by editing the environment and restarting. The token stays in the environment and never enters the database.</li></ul><p>See the <a href="/reference/configuration.html#integrations">configuration reference</a> for the variables.</p><h2 id="trusted-base-urls-and-private-hosts" tabindex="-1">Trusted base URLs and private hosts <a class="header-anchor" href="#trusted-base-urls-and-private-hosts" aria-label="Permalink to &quot;Trusted base URLs and private hosts&quot;">​</a></h2><p>A connection base URL is supplied by an administrator, so Piwi trusts it: a self-hosted tracker on a private network — <code>https://jira.internal.example.com</code> or an RFC 1918 address — works without extra configuration. Every URL a non-administrator supplies (pinning a link, for instance) still goes through the SSRF guard that blocks private hosts.</p><h2 id="what-unfurl-gives-today" tabindex="-1">What unfurl gives today <a class="header-anchor" href="#what-unfurl-gives-today" aria-label="Permalink to &quot;What unfurl gives today&quot;">​</a></h2><p>With a connection in place, a pinned or refreshed Jira link carries:</p><ul><li>the issue <strong>summary</strong> as its title;</li><li>a <strong>status</strong> badge whose color follows the issue&#39;s status category (to-do, in-progress, done);</li><li>a refresh that reads the ticket back through the connection rather than scraping the page.</li></ul><h2 id="creating-issues-from-a-failure" tabindex="-1">Creating issues from a failure <a class="header-anchor" href="#creating-issues-from-a-failure" aria-label="Permalink to &quot;Creating issues from a failure&quot;">​</a></h2><p>Once a connection exists, Piwi can <strong>file the ticket for you</strong> — a Jira issue whose body is the failure&#39;s fix plan, linked back as the cluster&#39;s known issue. See <a href="/features/issue-tracking.html">Issue tracking (Jira)</a> for the create flow, the fields, and how the key travels back into the inbox, notifications and pull-request comments.</p><p>A connection carries a <strong>default language</strong> for the tickets filed against it (a French Atlassian site can default them to French); a project binding overrides it, and the create modal offers a per-issue choice — see <a href="/features/issue-tracking.html#language">Language</a>.</p><h2 id="status-sync" tabindex="-1">Status sync <a class="header-anchor" href="#status-sync" aria-label="Permalink to &quot;Status sync&quot;">​</a></h2><p>A background task reads every tracked issue back through its connection and caches the status, title and assignee, so a closed ticket stops showing as open in the dashboard. It runs <strong>every 15 minutes by default</strong>; set <a href="/reference/configuration.html"><code>PIWI_INTEGRATIONS_SYNC_MINUTES</code></a> (1–1440) to change the cadence. Links on an open cluster refresh every sweep; links on a resolved or ignored cluster refresh at most once a day. A connection that fails to answer records the error on the connection card and does not stop the sweep. An administrator can run one sweep on demand with <code>POST /api/integrations/sync</code>; the response counts the links it refreshed, failed and skipped.</p><p>The two-way <strong>policies</strong> (comment on fix / regression, transition, resolve on close, reopen) are configured per project on the binding — see <a href="/features/issue-tracking.html#keep-the-ticket-honest">Keep the ticket honest</a>.</p><h2 id="registering-the-inbound-webhook" tabindex="-1">Registering the inbound webhook <a class="header-anchor" href="#registering-the-inbound-webhook" aria-label="Permalink to &quot;Registering the inbound webhook&quot;">​</a></h2><p>Polling is the baseline because Atlassian Cloud usually cannot reach a self-hosted Piwi. When it can, a Jira admin can register a webhook so a close or reopen reflects immediately instead of within the sync interval:</p><ol><li>In <strong>Settings → Integrations</strong>, on the Jira connection, choose <strong>Enable webhook</strong>. Piwi generates a per-connection secret and shows the full URL <strong>once</strong> — copy it now.</li><li>In Jira, go to <strong>Settings → System → Webhooks → Create a webhook</strong>, paste the URL, and subscribe it to <strong>issue updated</strong> events (optionally scoped by a JQL filter to the bound projects).</li><li>Piwi looks the issue up by its id in its links and refreshes just that one, applying the resolve/reopen policies.</li></ol><p>The webhook is <strong>public but secret-in-path</strong> and rate-limited, and it can only ever <em>refresh a link</em> — it can never create or transition anything. Regenerating the token invalidates the old URL; <em>Disable webhook</em> clears it.</p>',27)])])}const f=t(i,[["render",r]]);export{p as __pageData,f as default};

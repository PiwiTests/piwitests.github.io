import{_ as n,o,c as r,ak as a,j as t,a as s,t as l}from"./chunks/framework.sV-XtlWr.js";const m=JSON.parse('{"title":"AI steps","description":"","frontmatter":{"title":"AI steps","lang":"en-US"},"headers":[],"relativePath":"ai-steps.md","filePath":"ai-steps.md","lastUpdated":1788552316000}'),d={name:"ai-steps.md"};function h(i,e,p,c,g,k){return o(),r("div",null,[e[4]||(e[4]=a(`<h1 id="ai-steps" tabindex="-1">AI steps <a class="header-anchor" href="#ai-steps" aria-label="Permalink to &quot;AI steps&quot;">​</a></h1><p>AI steps let you locate elements and drive flows in <strong>plain English</strong>, without giving up the determinism of hand-written Playwright:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Find one element by description</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">piwiLocator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;the email address field&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fill</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ada@example.com&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Run a whole flow, with type-safe parameters</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">piwiRun</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sign in as {email}&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { email: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ada@example.com&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span></code></pre></div><p>The guiding principle is <strong>the LLM is a compiler, not a runtime</strong>. The first time a prompt is seen, an agent resolves it <strong>once</strong> into a committed, deterministic JSON artifact. Every run after that <em>replays</em> that artifact with ordinary Playwright calls — <strong>zero LLM calls and zero network</strong> in the default <code>replay</code> mode. Your CI stays fast, offline, and reproducible; the model is only ever involved while authoring.</p><div class="tip custom-block"><p class="custom-block-title">This composes with the capture fixtures</p><p>AI steps are a separate opt-in from the <a href="./capture-fixtures.html">capture fixtures</a>, and compose with them. Replayed actions flow through the instrumented page, so they feed traces, reports, and <a href="./reporter.html#locator-healing">locator healing</a> exactly like hand-written code.</p></div><h2 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h2><p>Extend your test with <code>extendPiwiAi</code>. Compose it over <code>extendPiwiFixtures</code> to get the capture fixtures too:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// tests/fixtures.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { test </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> base } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@playwright/test&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { extendPiwiFixtures, extendPiwiAi } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@piwitests/reporter&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// AI steps + capture fixtures</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> test</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> extendPiwiAi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">extendPiwiFixtures</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(base))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { expect } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@playwright/test&#39;</span></span></code></pre></div><p><code>page</code> now carries two extra methods:</p><table tabindex="0"><thead><tr><th>Method</th><th>Returns</th><th>Use for</th></tr></thead><tbody><tr><td><code>page.piwiLocator(template)</code></td><td>a real, synchronous <code>Locator</code></td><td>locating one element by description</td></tr><tr><td><code>page.piwiRun(template)</code></td><td><code>Promise&lt;void&gt;</code></td><td>replaying a compiled multi-step flow</td></tr></tbody></table><p>Placeholders are type-checked at compile time: a template with <code>{param}</code> <strong>requires</strong> a matching params object, and a misspelled or missing name is a TypeScript error.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">piwiRun</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;add {qty} of {sku} to the cart&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { qty: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;2&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, sku: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;PIWI-1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span></code></pre></div><h2 id="the-lifecycle-author-once-replay-forever" tabindex="-1">The lifecycle: author once, replay forever <a class="header-anchor" href="#the-lifecycle-author-once-replay-forever" aria-label="Permalink to &quot;The lifecycle: author once, replay forever&quot;">​</a></h2><p>An AI step moves through three modes, set with the <code>ai.mode</code> option or <code>PIWI_AI</code>:</p><table tabindex="0"><thead><tr><th>Mode</th><th>What it does</th></tr></thead><tbody><tr><td><code>replay</code> <em>(default)</em></td><td>Executes the committed artifact read-only. <strong>Never</strong> calls the model. Fails closed on a missing entry.</td></tr><tr><td><code>resolve</code></td><td>Authors any missing entry by driving the model, verifies it against the live page, and writes it to disk.</td></tr><tr><td><code>heal</code></td><td>Repairs a committed entry that no longer replays (e.g. a renamed element), without re-authoring from scratch.</td></tr></tbody></table><p>The intended workflow:</p><ol><li><strong>Write</strong> the test with <code>piwiLocator</code> / <code>piwiRun</code> prompts.</li><li><strong>Author</strong> locally in <code>resolve</code> mode (see below). The agent produces a JSON artifact next to your spec under <code>__piwi__/</code>.</li><li><strong>Commit</strong> that artifact to git alongside the test.</li><li><strong>Replay</strong> everywhere else — locally and in CI — with zero model calls.</li></ol><h3 id="authoring-resolve-mode" tabindex="-1">Authoring (resolve mode) <a class="header-anchor" href="#authoring-resolve-mode" aria-label="Permalink to &quot;Authoring (resolve mode)&quot;">​</a></h3><p>Authoring needs a running Piwi dashboard with an AI provider configured (<strong>Settings → AI</strong>, or the <code>PIWI_AI_PROVIDER</code> <a href="./configuration.html#ai-diagnosis">environment variables</a>). The reporter sends each iteration to the server&#39;s resolution endpoint; the server calls the model and returns one decision, which the reporter compiles deterministically. <strong>API keys stay on the server.</strong></p><p>Point the reporter at that server and run the suite in resolve mode:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PIWI_DASHBOARD_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://piwi.example.com&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # the authoring server</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PIWI_API_KEY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                # a reporter API key</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PIWI_AI</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">resolve</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> playwright</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # or: npx piwi ai resolve --grep &quot;sign in&quot;</span></span></code></pre></div><p><code>piwi ai resolve</code> runs the suite in resolve mode for you (forcing <code>--workers=1</code> so authoring is serial). Once it finishes, review and <strong>commit</strong> the generated files.</p><div class="warning custom-block"><p class="custom-block-title">Authoring drives the real page</p><p>In <code>resolve</code> mode each step is executed against the live page with your real parameter values so the flow can advance. Author against a disposable environment, not production.</p></div><h3 id="replay-and-ci" tabindex="-1">Replay and CI <a class="header-anchor" href="#replay-and-ci" aria-label="Permalink to &quot;Replay and CI&quot;">​</a></h3><p>In <code>replay</code> mode a missing entry is a hard, actionable failure — the run does not silently pass and does not reach for a model:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>piwi AI: no committed entry for &quot;sign in as {email}&quot; in test &quot;checkout&quot;.</span></span>
<span class="line"><span>  expected file: tests/__piwi__/checkout.spec.ts/checkout.sign-in-as-email.a1b2c3d4.json</span></span>
<span class="line"><span>  run:  piwi ai resolve --grep &quot;checkout&quot;</span></span></code></pre></div><p>That means an environment with no model access (a locked-down CI runner, an air-gapped box) can never accidentally author — it either replays a committed artifact or fails. If you would rather a missing entry mark the test <strong>fixme</strong> (yellow) instead of failing red, set <code>ai.onMiss: &#39;fixme&#39;</code> (<code>PIWI_AI_ON_MISS=fixme</code>).</p><p>Because replay is plain Playwright, there is nothing extra to install or start in CI. Commit the artifacts and your existing test job replays them.</p><h2 id="what-s-inside-an-artifact-—-and-why-it-s-safe" tabindex="-1">What&#39;s inside an artifact — and why it&#39;s safe <a class="header-anchor" href="#what-s-inside-an-artifact-—-and-why-it-s-safe" aria-label="Permalink to &quot;What&#39;s inside an artifact — and why it&#39;s safe&quot;">​</a></h2><p>An entry is <strong>data, never code</strong>: an allowlisted locator program plus, for a flow, an ordered list of steps and a postcondition oracle. Determinism and safety come from several guarantees:</p><ul><li><strong>Deterministic bytes.</strong> The model only ever <em>names</em> an element (its ARIA role + accessible name). The <a href="./concepts.html#locator-snapshot"><code>@piwitests/core</code></a> scorer turns that into the committed locator, so model sampling never changes the file. Two runs that reach the same conclusion produce byte-identical JSON, and a no-op re-resolution leaves your working tree clean.</li><li><strong>No evaluation.</strong> Every locator method and action is checked against an allowlist before it touches the page. A tampered or malformed artifact can never become arbitrary execution.</li><li><strong>Drift guard.</strong> Each step records the element&#39;s role/name at author time. On replay, if the page positively shows that element has drifted (a rename), the flow <strong>stops before acting</strong> rather than clicking the wrong thing.</li><li><strong>Postcondition oracle.</strong> Every flow ends with an assertion the agent chose (an element became visible/hidden, or the URL changed). Replay verifies it, so a subtly wrong flow fails loudly instead of passing.</li><li><strong>Ajax waiting, race-proof.</strong> A step can wait for a network response it triggers (an XHR/fetch the agent observed while authoring). The wait is armed <em>before</em> the action fires, so a fast reply can never slip through the gap.</li></ul><h2 id="intent-in-the-dashboard" tabindex="-1">Intent in the dashboard <a class="header-anchor" href="#intent-in-the-dashboard" aria-label="Permalink to &quot;Intent in the dashboard&quot;">​</a></h2><p>Each test&#39;s replayed AI steps are reported to the dashboard as a small usage manifest: the committed artifacts it exercised (powering the project&#39;s <strong>AI steps</strong> liveness tab) plus <strong>intent mappings</strong> — each compiled locator paired with the prompt it came from. Two places use them:</p><ul><li><strong>Locator healing</strong>: when the failing locator was compiled from a prompt, the healing panel shows it — <em>&quot;Compiled from prompt: &#39;the email address field&#39;&quot;</em> — so you fix the intent, not just the selector.</li><li><strong>AI diagnosis</strong>: the diagnosis context includes an <em>AI Steps</em> section listing the prompts behind the test&#39;s locators, so a root cause can be phrased in intent terms (&quot;the element the test calls <em>the email address field</em> was renamed&quot;). Capped by <code>PIWI_AI_MAX_STEP_INTENTS</code> (default 20, <code>0</code> disables).</li></ul>`,34)),t("p",null,[e[0]||(e[0]=s("Intent mappings are as private as everything else here: templates keep their ",-1)),e[1]||(e[1]=t("code",null,"{param}",-1)),e[2]||(e[2]=s(" placeholders and locators their ",-1)),t("code",null,l(i.param),1),e[3]||(e[3]=s(" markers — parameter values never appear.",-1))]),e[5]||(e[5]=a(`<h2 id="privacy" tabindex="-1">Privacy <a class="header-anchor" href="#privacy" aria-label="Permalink to &quot;Privacy&quot;">​</a></h2><p>Parameter values are <strong>masked out of everything sent to the model</strong>. The page snapshot the agent sees has your <code>{param}</code> values replaced with markers, and placeholders survive compilation as markers that are substituted locally at replay. Secrets in parameters never leave your machine. See <a href="./privacy.html">Privacy &amp; data flow</a> for the full picture.</p><h2 id="the-piwi-ai-cli" tabindex="-1">The <code>piwi ai</code> CLI <a class="header-anchor" href="#the-piwi-ai-cli" aria-label="Permalink to &quot;The \`piwi ai\` CLI&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>piwi ai check     Scan committed entries for orphans, non-canonical files and</span></span>
<span class="line"><span>                  duplicate templates. Read-only; exits 1 when issues are found.</span></span>
<span class="line"><span>piwi ai resolve   Author missing entries by running the suite in resolve mode.</span></span>
<span class="line"><span>piwi ai prune     Delete orphaned/dormant entries.</span></span></code></pre></div><p><code>piwi ai check</code> is offline and CI-friendly — add it as a lint step to catch an entry whose prompt was deleted or renamed, a file that isn&#39;t in canonical form, or two prompts that collide:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> piwi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # exit 1 on any hygiene issue</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> piwi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --json</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # machine-readable findings</span></span></code></pre></div><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Set these under the <code>ai</code> key of your reporter options (in <code>wrapConfig</code>&#39;s second argument, or the reporter entry options). Every option has an environment-variable equivalent.</p><table tabindex="0"><thead><tr><th>Option</th><th>Env var</th><th>Default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>ai.mode</code></td><td><code>PIWI_AI</code></td><td><code>replay</code></td><td><code>replay</code> / <code>resolve</code> / <code>heal</code>.</td></tr><tr><td><code>ai.dir</code></td><td><code>PIWI_AI_DIR</code></td><td><code>__piwi__</code></td><td>Per-spec directory holding committed entries.</td></tr><tr><td><code>ai.onMiss</code></td><td><code>PIWI_AI_ON_MISS</code></td><td><code>fail</code></td><td>On a replay miss: <code>fail</code> (red) or <code>fixme</code> (yellow).</td></tr><tr><td><code>ai.maxSteps</code></td><td><code>PIWI_AI_MAX_FLOW_STEPS</code></td><td><code>20</code></td><td>Max steps the agent may take authoring one flow.</td></tr><tr><td><code>ai.maxSnapshotChars</code></td><td><code>PIWI_AI_MAX_SNAPSHOT_CHARS</code></td><td><code>24000</code></td><td>Max ARIA-snapshot characters sent per authoring iteration.</td></tr><tr><td><code>ai.optionalProbeTimeout</code></td><td><code>PIWI_AI_OPTIONAL_PROBE_TIMEOUT</code></td><td><code>2000</code></td><td>Existence-probe timeout (ms) for an <code>optional</code> step during replay.</td></tr><tr><td><code>ai.responseWaitTimeout</code></td><td><code>PIWI_AI_RESPONSE_WAIT_TIMEOUT</code></td><td>Playwright default</td><td>Timeout (ms) for a step&#39;s <code>waitForResponse</code>, and the authoring settle window.</td></tr><tr><td><code>ai.screenshotFallback</code></td><td><code>PIWI_AI_SCREENSHOT_FALLBACK</code></td><td><code>false</code></td><td>Send a screenshot when the ARIA snapshot is empty (<strong>vision models only</strong>).</td></tr></tbody></table><p>Authoring also reads <code>PIWI_DASHBOARD_URL</code> and <code>PIWI_API_KEY</code> (the server that runs the model), and <code>PIWI_AI_UPDATE=true</code> (or <code>--update-ai</code>) forces re-authoring of entries that already exist.</p><p>On the <strong>server</strong> side, two limits bound each authoring iteration — see the <a href="./configuration.html#ai-steps">AI steps section</a> of the configuration reference:</p><ul><li><code>PIWI_AI_STEP_MAX_SNAPSHOT_CHARS</code> — snapshot characters the model receives.</li><li><code>PIWI_AI_STEP_MAX_OUTPUT_TOKENS</code> — output tokens the model may return. <strong>Reasoning models</strong> spend tokens on hidden chain-of-thought, so raise this (up to <code>8192</code>) when authoring with one, or the JSON decision can be truncated.</li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./reporter.html">Reporter</a> — installing and configuring the reporter.</li><li><a href="./capture-fixtures.html">Capture fixtures</a> — the sibling opt-in that powers healing and performance data.</li><li><a href="./ai-diagnosis.html">AI diagnosis</a> — the dashboard&#39;s failure-analysis AI (a different feature that reuses the same provider config).</li></ul>`,14))])}const y=n(d,[["render",h]]);export{m as __pageData,y as default};

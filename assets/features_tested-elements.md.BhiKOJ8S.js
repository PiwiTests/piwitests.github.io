import{_ as o,C as s,o as n,c as i,j as t,a as r,E as h,ak as l}from"./chunks/framework.sV-XtlWr.js";const b=JSON.parse('{"title":"Tested elements","description":"","frontmatter":{"title":"Tested elements","lang":"en-US"},"headers":[],"relativePath":"features/tested-elements.md","filePath":"features/tested-elements.md","lastUpdated":1790439044000}'),c={name:"features/tested-elements.md"};function d(p,e,g,m,u,w){const a=s("Needs");return n(),i("div",null,[e[0]||(e[0]=t("h1",{id:"tested-elements",tabindex:"-1"},[r("Tested elements "),t("a",{class:"header-anchor",href:"#tested-elements","aria-label":'Permalink to "Tested elements"'},"​")],-1)),h(a,{reporter:"",extension:""}),e[1]||(e[1]=l('<p>Open a page of your application, and see which of its elements your tests reach, through which tests, and which buttons, links and fields no test reaches. <strong>Tested elements</strong> is a tool of the <a href="./extension.html">Piwi Picker extension</a>, connected to your Piwi instance: open it from the toolbar popup, or press <code>T</code> with the popup open.</p><p>It reads the project&#39;s <a href="./locator-usage.html">locator index</a>, the locator chains your tests used in their steps, and evaluates each chain on the page the way Playwright would: roles and accessible names, text and label matching, test ids (with the project&#39;s <code>testIdAttribute</code>), CSS and XPath, <code>filter()</code>, <code>and()</code>/<code>or()</code>, <code>nth()</code>, open shadow roots and same-origin frames. Nothing about the page is sent to your instance: the index comes down, the matching happens in the tab.</p><h2 id="reading-the-overlay" tabindex="-1">Reading the overlay <a class="header-anchor" href="#reading-the-overlay" aria-label="Permalink to &quot;Reading the overlay&quot;">​</a></h2><ul><li><strong>Green boxes</strong> are elements tests act on: click, fill, check, select, press.</li><li><strong>Blue boxes</strong> are elements tests only assert on (<code>toBeVisible</code>, <code>toHaveText</code>, …).</li><li><strong>Dashed amber boxes</strong> are visible buttons, links and fields that no test reaches.</li></ul><p>Each box carries the number of tests that reach it; <strong>Heatmap</strong> shades the boxes by that number instead. A dotted border means every locator reaching the element matches several elements on this page: the test may use it on another page, or in a state where only one of them exists.</p><p>Hovering a box shows the locators and tests behind it, clicking pins that card, and each test links to its page in the dashboard. <strong>Find these locators in Piwi ↗</strong> opens the project&#39;s <a href="./locator-usage.html#the-locators-page">Locators page</a> with the element&#39;s locators already checked.</p><p>The side panel sums up the page: how many of its interactive elements a test reaches, how many none does, and how many tests have locators resolving here. Its three tabs list the matched elements, the tests, and the elements no test reaches, each with a filter. A row of the last tab copies a ranked locator for that element, to start writing its test. The page is re-checked as it changes, so opening a menu or a dialog adds what the tests reach inside it.</p><h2 id="one-element-at-a-time" tabindex="-1">One element at a time <a class="header-anchor" href="#one-element-at-a-time" aria-label="Permalink to &quot;One element at a time&quot;">​</a></h2><p><strong>Limit to an element</strong>, in the panel, narrows the view to one part of the page: point at it and click, with <code>↑</code> to widen the outline to its container and <code>↓</code> to narrow it back. The click that chooses never reaches the page, so a link or a button is chosen without being followed or pressed. The boxes, the summary and the three tabs then count only the element and what is inside it: a form, a card, a menu.</p><p><strong>Around it</strong> lists the tested elements that contain it: a test checking that the product card is visible reaches the card, not the button inside it, which is a weaker answer. Clicking one moves the view to that container, <strong>Container ↑</strong> does the same one level up, and <strong>Whole page</strong> or <strong>Esc</strong> drops the limit.</p><p>Picking an element with the extension, while connected, gives the same answer without opening the overlay: under its ranked locators, the tests that reach the element itself, those that reach only something inside it, and those that reach only a container around it, each with the actions they run. <strong>Show tested elements inside it</strong> opens the overlay limited to that element. <strong>Copy all</strong> copies every ranked locator, one per line, for the <a href="./locator-usage.html#the-locators-page">Locators page</a> of the dashboard.</p><h2 id="setting-it-up" tabindex="-1">Setting it up <a class="header-anchor" href="#setting-it-up" aria-label="Permalink to &quot;Setting it up&quot;">​</a></h2><ol><li>Connect the extension to your instance and map the application&#39;s URLs to its project: see <a href="./extension.html#connecting-to-a-piwi-instance">Connecting to a Piwi instance</a>. A mapping can name the branch deployed at those URLs, a staging site on <code>develop</code> for instance: the overlay then shows what that branch&#39;s tests reach, as the <a href="./locator-usage.html#branches">Locators page</a> reads a branch. The branch select in the panel changes it for the rest of the browser session.</li><li>Let a few runs arrive. The index fills in from the steps the reporter already sends, with Playwright 1.61 or later: see the <a href="./locator-usage.html#requirements-and-limits">requirements</a>.</li><li>For projects that set <code>testIdAttribute</code> in their Playwright config, the reporter records it with each run, and <code>getByTestId</code> reads that attribute on the page.</li></ol><p>The extension keeps a copy of the index for a minute, then downloads it again in the background; the panel&#39;s <strong>Refresh</strong> downloads it now.</p><h2 id="what-it-does-not-tell-you" tabindex="-1">What it does not tell you <a class="header-anchor" href="#what-it-does-not-tell-you" aria-label="Permalink to &quot;What it does not tell you&quot;">​</a></h2><ul><li><strong>Where the test used the locator.</strong> The index keeps the chain, not the page URL of the step, so a chain is matched on whatever page you&#39;re on. <code>getByRole(&#39;button&#39;, { name: &#39;Save&#39; })</code> used on the settings page also outlines a <strong>Save</strong> button on the profile page. The card lists the call sites, so you can check.</li><li><strong>States the page is not in.</strong> Only what is in the DOM now is matched: a closed menu, another step of a wizard, or content behind a login shows once the page shows it.</li><li><strong>Locators the index skips</strong>: chains cut short, <code>locator.describe()</code>, and locators used only through getters such as <code>textContent()</code>, as listed in the <a href="./locator-usage.html#requirements-and-limits">index limits</a>.</li><li><strong>Cross-origin frames</strong>, which an extension&#39;s page script cannot read, and a few selectors that depend on layout (<code>:left-of()</code>, <code>:near()</code>, …), listed under the panel&#39;s <strong>Notes</strong>.</li><li><strong>While a modal dialog is open</strong>, the browser keeps the rest of the page inert: the boxes stay visible, and the panel works again once the dialog closes.</li></ul><p>A green box says a test reached the element, not that the test checks what matters about it. Use it to find what nothing reaches, and to see which tests to read before changing an element.</p>',17))])}const y=o(c,[["render",d]]);export{b as __pageData,y as default};

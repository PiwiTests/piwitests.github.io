import{_ as s,C as r,o,c as n,j as t,a as i,E as h,ak as d}from"./chunks/framework.sV-XtlWr.js";const y=JSON.parse('{"title":"Analytics widgets","description":"","frontmatter":{"title":"Analytics widgets","lang":"en-US"},"headers":[],"relativePath":"features/analytics-widgets.md","filePath":"features/analytics-widgets.md","lastUpdated":1790422632000}'),l={name:"features/analytics-widgets.md"};function c(p,e,g,u,m,f){const a=r("Needs");return o(),n("div",null,[e[0]||(e[0]=t("h1",{id:"analytics-widgets",tabindex:"-1"},[i("Analytics widgets "),t("a",{class:"header-anchor",href:"#analytics-widgets","aria-label":'Permalink to "Analytics widgets"'},"​")],-1)),h(a,{reporter:""}),e[1]||(e[1]=d('<p>Every widget of the <a href="./analytics.html">Analytics</a> page and of the <a href="./dashboards.html">dashboards</a> you save, with what it counts. Each one follows the scope bar; a widget that cannot follow a test filter says so under its title.</p><h2 id="where-things-stand" tabindex="-1">Where things stand <a class="header-anchor" href="#where-things-stand" aria-label="Permalink to &quot;Where things stand&quot;">​</a></h2><p><strong>Headline numbers</strong> — six tiles across every project in scope (test pass rate, run success rate, flaky tests, wasted CI minutes, open failure causes, median time to fix), each with its change against the comparison. With <a href="./analytics.html#targets">targets</a> set, a tile says whether its target is met. <strong>Pass rate over time</strong> draws the comparison period as a faint line, with markers and, for one project, its target.</p><p><strong>Portfolio health</strong> — one sortable row per project: pass rate and its change vs the previous period, flaky volume, open failure clusters, average run duration, and latest run. Worst health sorts first.</p><p><strong>Insights</strong> — a severity-ranked feed of what changed: missed targets, pass-rate drops, failing streaks, stale failure clusters, wasted CI time, oversized timeouts and stale <code>test.slow()</code> marks, regression surges, slow shared endpoints, a median time to fix half again as long, a suite that lost tests, a growing quarantine, and one owner holding more than half the open failure causes. Each entry links to what is behind it.</p><p><strong>Pass rate heatmap</strong> — projects × time, colored by the pass rate of each UTC day (or wider bucket, over longer periods). This is the fastest way to answer <em>when</em> something started degrading.</p><p><strong>Narrative</strong> — off by default; add it to a dashboard, or turn on <strong>AI narrative</strong> in a <a href="./quality-reports.html#report-schedules">report schedule</a>, which puts it at the top of each report. In a scheduled quality report, the configured <a href="./ai-diagnosis.html">AI diagnosis</a> model writes three paragraphs in the report language from the report&#39;s numbers alone, labeled as generated. An answer that cites a number the report does not hold is refused. Everywhere else, and whenever no model is configured or it fails, the widget shows the rule-based verdict. The verdict and the tiles stay the report&#39;s spine.</p><p>Every pass rate in the dashboard is colored on the same scale: green at 90% or more, amber from 50%, red below 50%. The heatmap and the browser matrix split the green and amber bands into two shades each, so a perfect period and a nearly failing one stand out.</p><h2 id="where-the-pain-is" tabindex="-1">Where the pain is <a class="header-anchor" href="#where-the-pain-is" aria-label="Permalink to &quot;Where the pain is&quot;">​</a></h2><p><strong>Failure clusters</strong> — open root causes across all projects by age, occurrences, and error-type mix, with the oldest unresolved cluster highlighted.</p><p><strong>Flakiest tests</strong> — the global flaky leaderboard, using the same <a href="./flaky-tests.html#impact-ranking">scoring and impact ranking</a> as each project&#39;s Flaky tests tab.</p><p><strong>CI time</strong> and <strong>Wasted CI time</strong> — total minutes your runs consumed, and how many of those produced no signal: time spent inside wait steps plus time spent executing attempts that ended failed or timed out. Because a timed-out test burns its entire (often oversized) budget, the widget also calls out how much is reclaimable by tightening timeouts and removing stale <code>test.slow()</code> marks. With a <a href="./quality-reports.html#cost-of-a-ci-minute">cost of a CI minute</a> set, it shows the money too.</p><p><strong>Time to fix</strong> — how fast you react: failure causes opened and fixed per bucket, the median and p90 time from first failure to fix over the causes fixed in the period, the share of those fixes that held, and the open causes by age. Failure clusters outlive run retention, so this reaches back as far as they do.</p><p><strong>Ownership</strong> — one row per owner: the open failure causes assigned to them, the flaky tests and wasted CI minutes of the tests they own (their <code>piwi:owner</code> annotation), and the median time to fix of the causes they fixed, with an <strong>Unowned</strong> row for everything nobody holds. It answers &quot;which team&quot;, and pairs with the owner test filter of the Team quality report.</p><h2 id="which-way-it-is-going" tabindex="-1">Which way it is going <a class="header-anchor" href="#which-way-it-is-going" aria-label="Permalink to &quot;Which way it is going&quot;">​</a></h2><p><strong>Regression velocity</strong> — new regressions and newly-flaky tests introduced per period, stacked, with the change vs the previous period.</p><p><strong>Suite growth</strong> — how many tests the suite has over time (the highest test count one run reported in each bucket), its change against the comparison period, and the share of tests skipped or not run. A suite that grows while its skipped share grows too is adding tests nobody runs.</p><p><strong>Flaky debt</strong> — whether flakiness is going down since you started fixing it: flaky occurrences per run in each bucket, the distinct flaky tests of each bucket (from the stored runs, so as far back as retention keeps them) and the tests in quarantine at the end of each bucket.</p><h2 id="detail" tabindex="-1">Detail <a class="header-anchor" href="#detail" aria-label="Permalink to &quot;Detail&quot;">​</a></h2><p><strong>Browser matrix</strong> — pass rate per project × browser, so a suite that&#39;s green on Chromium and failing on WebKit stands out.</p><p><strong>Slow endpoints</strong> — backend calls captured during tests, aggregated across all projects by normalized route: p50/p90 latency, error rate, and how many projects hit each one. A shared endpoint regressing shows up here before it&#39;s obvious in any single suite. Requires the <a href="/guide/capture-fixtures.html">capture fixtures</a>.</p><p><strong>Environment comparison</strong> — test pass rate and run success per environment, side by side with their change against the comparison period, and each environment&#39;s pass rate over time: &quot;staging is green and production is not&quot;.</p><p><strong>Movers</strong> — the test-level &quot;what changed&quot;: tests that became flaky or stopped being flaky, and tests whose average passing duration grew or shrank by more than 25 %, against the comparison period. It reads the stored runs, so both periods reach back only as far as retention keeps them, and each direction lists its top tests, at most 25.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./analytics.html">Analytics</a>: the scope, periods, targets and chart export</li><li><a href="./dashboards.html">Dashboards</a>: arranging these widgets into views of your own</li><li><a href="./quality-reports.html">Quality reports</a>: any dashboard as a document</li></ul>',25))])}const b=s(l,[["render",c]]);export{y as __pageData,b as default};
